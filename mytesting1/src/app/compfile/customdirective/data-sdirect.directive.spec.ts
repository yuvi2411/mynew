import { DataSdirectDirective } from './data-sdirect.directive';

describe('DataSdirectDirective', () => {
  it('should create an instance', () => {
    const directive = new DataSdirectDirective();
    expect(directive).toBeTruthy();
  });
});
